apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calculate-journey-variable-payments.fullname" . }}-test-connection"
  labels:
    {{- include "calculate-journey-variable-payments.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calculate-journey-variable-payments.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
